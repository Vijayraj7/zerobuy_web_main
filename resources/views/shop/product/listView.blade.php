<div class="table-responsive">
    <table class="table border-left-right table-responsive-lg datatableCustomCSS" id="shopProductTable">
        <thead>
            <tr>
                <th class="text-center">{{ __('SL') }}</th>
                <th>{{ __('Create Date') }}</th>
                <th>{{ __('Product ID') }}</th>
                <th style="min-width: 150px">{{ __('Product Name') }}</th>
                <th>{{ __('Image') }}</th>
                <th class="text-center">{{ __('Qty') }}</th>
                <th class="text-center">{{ __('MRP') }}</th>
                <th class="text-center" style="min-width: 120px">{{ __('Selling Price') }}</th>
                <th class="text-center">{{ __('Total Sales') }}</th>
                <th class="text-center">{{ __('Variants / Bulk Items') }}</th>
                <th class="text-center">{{ __('Status') }}</th>
                <th class="text-center">{{ __('Action') }}</th>
            </tr>
        </thead>
    </table>
</div>
